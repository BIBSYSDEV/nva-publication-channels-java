plugins {
    id 'nva.publication.channels.java.rootplugin'
}

dependencies {
}

test {
    environment("ALLOWED_ORIGIN", "*")
    environment("CHANNEL_REGISTER_CACHE_BUCKET", "someBucket")
    environment("CHANNEL_REGISTER_CACHE_S3_OBJECT", "someFile")
    environment("TABLE_NAME", "someTable")
    environment("APPLICATION_CONFIG_NAME", "ApplicationConfig")
    environment("APPLICATION_CONFIG_PROFILE_NAME", "ApplicationConfigurationProfile")
    environment("APPLICATION_CONFIG_ENVIRONMENT_NAME", "Live")
    environment("AWS_APPCONFIG_EXTENSION_PREFETCH_LIST", "path/path/path")
}
